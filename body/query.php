<?php
    // ↓ Информация о правах пользователя ↓
//    if ($_COOKIE['user'] != 'admin')
//    {
//        $USER->user_permission($substring);
//        $vision = $USER->user_cell_vision;                  // Список видимых пользователю полей (по ключу)
//        $permission = $USER->user_cell_edit;                // Список полей, доступных для редактирования
//        $title = $USER->user_cell_vision_name;              // Список заголовков таблицы
//    }
    //$TABLE->current_table($substring,$vision);
    //$data = $TABLE->table_data;                         // Список данных для заполнения таблицы
    // ↑ Информация о правах пользователя ↑
?>